
import React from "react";
import "./Loader.css"; // Create a separate CSS file for styling the loader

const Loader = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
